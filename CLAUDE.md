# WeekendCourse — 주말 데이트 코스 앱

SNS 스크린샷을 공유하면 장소를 자동 정리하고 매주 금요일 저녁에 주말 데이트 코스를 알림으로 보내주는 Android 앱.

## 기술 스택
- **언어**: Kotlin 2.0.21
- **UI**: Jetpack Compose (Material 3), Compose BOM 2024.12.01
- **아키텍처**: MVVM + Repository, 단일 모듈
- **DI**: Hilt 2.52 (KSP)
- **DB**: Room 2.7.0
- **비동기**: Coroutines + Flow
- **네트워킹**: Retrofit 2.11.0 + OkHttp 4.12.0 + kotlinx.serialization 1.7.3
- **OCR**: ML Kit Text Recognition Korean 16.0.1 (온디바이스)
- **이미지**: Coil 2.7.0
- **백그라운드**: WorkManager 2.10.0
- **minSdk**: 26 / **targetSdk**: 35

## 패키지 구조
```
com.pinkanimal.weekendcourse
├─ di/                    Hilt 모듈
├─ data/
│  ├─ local/              Room: Entity, Dao, Database
│  ├─ remote/             Retrofit: ClaudeApi, KakaoApi, DTO
│  └─ repository/         PlaceRepository, LlmRepository, MapRepository, CourseRepository
├─ domain/                모델, 유즈케이스
├─ ocr/                   ML Kit 래퍼 + 휴리스틱 게이트
├─ work/                  WorkManager: FridayDigestWorker
├─ ui/
│  ├─ share/              공유 수신 → 확인 화면
│  ├─ list/               저장한 장소 목록
│  ├─ course/             주말 코스 화면
│  └─ theme/
└─ MainActivity.kt
```

## 빌드 방법

1. `local.properties` 파일 생성 (절대 커밋 금지):
   ```
   sdk.dir=/path/to/android/sdk
   CLAUDE_API_KEY=sk-ant-...
   KAKAO_REST_API_KEY=...
   ```
2. `./gradlew assembleDebug`

## Phase 계획
- **Phase 0** ✅ — 프로젝트 스캐폴드, Hilt/Compose/Room/Retrofit 의존성
- **Phase 1** — 공유 인텐트 수신 → 이미지 복사 → ML Kit OCR → raw 텍스트 표시
- **Phase 2** — 휴리스틱 게이트 + Claude 추출 + 확인 화면 + Room 저장 + 목록 화면
- **Phase 3** — Kakao Local 정규화 (좌표·구·영업시간)
- **Phase 4** — WorkManager 금요일 19:00 + 코스 생성 + 알림
- **Phase 5** — 카카오맵 딥링크 + UI 다듬기

## 외부 API
- **Claude API**: `https://api.anthropic.com/v1/messages`, 모델 `claude-haiku-4-5`
- **Kakao Local**: 키워드 장소 검색, `Authorization: KakaoAK {KEY}` 헤더

---

# 코딩 가이드라인 (범용)

흔한 LLM 코딩 실수를 줄이기 위한 행동 지침. 위 프로젝트 전용 지침과 함께 적용한다.

**트레이드오프**: 이 지침들은 속도보다 신중함을 우선한다. 사소한 작업에는 판단력을 발휘할 것.

## 1. 코딩 전에 생각하기

**추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현하기 전에:
- 가정을 명시적으로 서술한다. 불확실하면 물어본다.
- 여러 해석이 가능하면 제시한다 — 말없이 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반박한다.
- 불명확한 부분이 있으면 멈춘다. 무엇이 헷갈리는지 명시하고 물어본다.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드. 추측성 코드는 없다.**

- 요청받지 않은 기능은 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 발생할 수 없는 시나리오에 대한 에러 처리를 하지 않는다.
- 200줄을 작성했는데 50줄로 될 수 있다면 다시 쓴다.

스스로에게 물어볼 것: "시니어 엔지니어가 보기에 과하게 복잡한가?" 그렇다면 단순화한다.

## 3. 외과적 수정

**꼭 필요한 부분만 건드릴 것. 자신이 만든 문제만 치울 것.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷팅을 "개선"하지 않는다.
- 깨지지 않은 것을 리팩터링하지 않는다.
- 자신의 취향과 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 지우지 않는다.

변경으로 인해 고아가 생기면:
- 자신의 변경으로 인해 쓰이지 않게 된 import/변수/함수는 제거한다.
- 요청받지 않았다면 기존에 있던 죽은 코드는 제거하지 않는다.

테스트: 변경된 모든 줄이 사용자의 요청과 직접 연결되어야 한다.

## 4. 목표 지향 실행

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 변환한다:
- "검증 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시킨다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과하는지 확인한다"

다단계 작업에는 간단한 계획을 서술한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

강한 성공 기준은 독립적으로 반복 작업을 가능하게 한다. 약한 기준("동작하게 만들기")은 끊임없는 확인을 필요로 한다.

## 5. 콜론으로 문장을 끝내지 않기 (한국어 출력)

**한국어 문장은 콜론이 아니라 마침표로 끝낸다.**

사용자가 한국어로 쓰면 출력도 한국어다:
- 다음 줄이 목록이나 예시여도 문장을 `:`로 끝내지 않는다.
- 영어 문서로 학습된 LLM은 콜론 습관을 한국어에 흘린다. 이를 잡아낸다.
- 테스트: 모든 한국어 문장 종결부는 `.`, `?`, `!` 중 하나여야 하며 `:`가 아니어야 한다.
- 코드, 키-값 쌍, 라벨 안의 콜론은 괜찮다. 문장을 끝내는 용도만 아니면 된다.

## 6. 새 파일 헤더 주석 (한국어)

**새로 만드는 모든 소스 파일의 첫 줄: 그 파일의 역할을 설명하는 한국어 한 줄 주석.**

새 파일을 만들 때:
- TypeScript/JavaScript: `// 사용자 인증 상태를 관리하는 Context Provider`
- Python: `# KIS API 호출을 비동기로 래핑하는 클라이언트`
- SQL: `-- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰`
- 필수 지시문(`'use client'`, `'use server'`, shebang) 바로 아래에 위치시킨다.
- 설정 파일(`*.config.ts`, `package.json` 등)은 건너뛴다.

이유: 에이전트는 코드베이스를 선택적으로 읽지, 전체를 읽지 않는다. 한 줄짜리 한국어 헤더가 즉각적인 맥락을 제공해, 다음 세션(사람이든 에이전트든)이 전체 코드를 다시 읽지 않고도 탐색할 수 있게 한다.

## 7. 계획 + 체크리스트 + 컨텍스트 노트

**사소하지 않은 작업 전에는 세 가지 산출물을 만든다. 이것 없이 코딩을 시작하지 않는다.**

- **계획(Plan)** — 무엇을 왜 만드는지.
- **체크리스트(`checklist.md`)** — 구체적인 작업을 체크박스로. 진행하면서 체크한다.
- **컨텍스트 노트(`context-notes.md`)** — 작업 중 내린 결정과 그 이유. 계속 추가한다.

사용자가 계획만 주고 바로 코딩을 시작하라고 하면, 멈추고 물어본다: "체크리스트와 컨텍스트 노트를 먼저 만들까요?" 다음 세션(자신이든 다른 누구든)은 모든 결정을 다시 도출하지 않고 이어서 작업하기 위해 이 노트가 필요하다.

## 8. 완료 표시 전에 테스트 실행

**코드를 건드렸다면 "완료"라고 말하기 전에 테스트를 실행한다.**

- `npm test`, `pytest`, `cargo test` 등 프로젝트가 쓰는 도구를 실행한다.
- 테스트가 통과하면 결과를 보고한다. 실패하면 고치고 다시 실행한다.
- 테스트 설정이 없다면 최소한 프로젝트가 빌드/컴파일되는지 확인한다.
- 사용자가 "끝", "완료", "다 됐어"라고 신호하기 전에 미리 테스트를 실행한다.

이것은 LLM이 가장 자주 건너뛰는 단계다. 타협 불가로 취급한다.

## 9. 시맨틱 커밋

**논리적으로 하나의 변경이 끝났을 때 커밋한다. 사용자가 요청할 때까지 기다리지 않는다.**

- 테스트: "이 커밋을 한 문장으로 설명할 수 있는가?" 그렇다면 커밋한다. 아니라면 변경 사항이 아직 섞여 있는 것이니 나눈다.
- 좋은 예: "auth 미들웨어 추가". 나쁜 예: "auth 추가하고 UI도 고치고 버그도 수정" (3개로 나눌 것).
- 20개의 관련 없는 수정을 쌓아두고 개별적으로 롤백할 수 없게 만들지 않는다.
- 커밋을 위한 커밋은 하지 않는다 — 의미 있는 단위만 커밋한다.

참고: 혼자 하는 프로토타입이나 일회성 스크립트는 속도가 느려진다면 커밋을 느슨하게 묶어도 된다. 핵심은 되돌릴 수 있음이지, 격식이 아니다.

## 10. 에러를 읽을 것, 추측하지 말 것

**실제 에러/로그 줄을 읽는다. 기억으로 패턴 매칭하지 않는다.**

무언가 실패했을 때:
- 전체 에러 메시지와 스택 트레이스를 읽는다.
- 실제 로그 출력을 확인한다, 이렇게 나와야 한다고 가정하는 것이 아니라.
- 원인을 확인하기 전에 "흔한 수정법"을 적용하지 않는다.
- 불명확하면 print/log를 추가해 상태를 확인한 뒤 고친다.

이것은 "테스트 실행" 다음으로 LLM이 가장 자주 건너뛰는 단계다. 에러 키워드로 패턴 매칭해 가장 최근 패턴의 수정을 적용한다. 그렇게 한 줄짜리 버그가 세 개 파일짜리 리팩터링이 된다.

---

**이 지침들이 효과가 있다는 신호**: diff에 불필요한 변경이 줄어들고, 과도한 설계로 인한 재작성이 줄어들며, 실수 이후가 아니라 구현 전에 명확화 질문이 나온다.
